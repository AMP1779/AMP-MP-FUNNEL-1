{
  "name": "amp-producer-funnel",
  "version": "1.2.0",
  "private": true,
  "description": "AMP Producer Registration Funnel — Phase 1 (PostgreSQL persistence + durable interview media)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
